name: instagram_app
description: İnstagram Cloning UI Design

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  google_fonts: ^3.0.1
  cached_network_image: ^3.2.1
  modal_bottom_sheet: ^2.1.0
  bottom_sheet: ^3.1.1
  readmore: ^2.2.0
  pinch_zoom_image_last: ^0.4.1
  contained_tab_bar_view: ^0.8.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:

      # instagram logo resim adresleri
    - assets/images/logo_img/instagram_logo_img/instagram_logo.jpg # instagram logosu
    - assets/images/logo_img/instagram_logo_img/instagram_meta_logo.png # from meta logosu

     # instagram yazı resim adresi

      # uygulama ikon resim adresleri 
    
       # üst appbar bölümü ikonları
    - assets/images/logo_img/app_icons/message_icon.png # mesaj ikonu
    - assets/images/logo_img/app_icons/like_nofication.png # bildirim & beğeni ikonu
    - assets/images/logo_img/app_icons/post_add.png # ekleme ve oluşturma ikonu

       # alt bottom menü ikonları
    - assets/images/logo_img/app_icons/home_icon.png # anasayfa ikonu
    - assets/images/logo_img/app_icons/search_discover_icon.png # arama & keşfet sayfa yönlendirme ikonu
    - assets/images/logo_img/app_icons/reels_video_icon.png # reels video ikonu
    - assets/images/logo_img/app_icons/shopping_bag.png # alışveriş ikonu
    
       # Gönderi kutusu ikonları
    - assets/images/logo_img/app_icons/comment_icon.png # yorum görüntüleme ikonu
    - assets/images/logo_img/app_icons/share_send_icon.png # paylaşma ikonu
    - assets/images/logo_img/app_icons/post_saved_icon.png # kaydetme ikonu
    - assets/images/logo_img/app_icons/icons8-menu-vertical-50.png # dikey 3 nokta ikonu

     # Diğer Kullanıcıların ve Kullanıcının Paylaşmış Olduğu Gönderi Resim Adresleri

    - assets/images/post_img/user_own_posts_images/lofi_generator.png 

    
     # Kullanıcı Kendi Profil Resmi Adresi
    - assets/images/user_profile_img/user_own_profile_img/user_own_profile_img.jpg


    # Profil Sayfası Tab bar view ikonları

    - assets/images/logo_img/app_icons/icons8-name-64.png # post listesi ikon adresi


    # Profil Sayfası Sağ Üst Menü Listesi İkon Adresleri

    - assets/images/logo_img/app_icons/icons8-settings-50.png # ayarlar ikonu
    - assets/images/logo_img/app_icons/icons8-time-machine-50.png # arşiv ikonu
    - assets/images/logo_img/app_icons/icons8-bar-chart-50.png # istatistikler ikonu
    - assets/images/logo_img/app_icons/icons8-rollback-50.png # haraketlerin ikonu
    - assets/images/logo_img/app_icons/icons8-qr-code-50.png # QR Code ikonu  
    - assets/images/logo_img/app_icons/instagram-save.png # Kaydedilenler ikonu  




  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
